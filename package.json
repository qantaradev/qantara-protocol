{
  "name": "qantara",
  "version": "0.1.0",
  "private": true,
  "description": "Web3 payment protocol for Solana with automatic buyback-and-burn",
  "workspaces": [
    "apps/*",
    "sdk",
    "contracts"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev",
    "setup": "node scripts/setup.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
